# JWT_SECRET: Private secret key used to sign and verify session tokens for the admin dashboard.
# In development, set this to a local secret key in .env or .env.local.
# In production, set this to a long random string in your hosting provider's environment variables or secrets.
# This secret must never be committed to the source repository.
JWT_SECRET=
